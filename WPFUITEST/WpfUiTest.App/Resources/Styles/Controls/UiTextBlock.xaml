<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <!-- 1号标题 TextBlock -->
    <Style
        x:Key="TitleOneUiTextBlockStyle"
        TargetType="{x:Type TextBlock}">
        <!-- 字号 -->
        <Setter Property="FontSize" Value="32" />
        <!-- 字体加粗 -->
        <Setter Property="FontWeight" Value="Bold" />
        <!-- 靠左居中对齐 -->
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <!-- 外边距 -->
        <Setter Property="Margin" Value="0,0,0,15" />
    </Style>

    <!-- 2号标题 TextBlock -->
    <Style
        x:Key="TitleTwoUiTextBlockStyle"
        TargetType="{x:Type TextBlock}">
        <!-- 字号 -->
        <Setter Property="FontSize" Value="24" />
        <!-- 字体加粗 -->
        <Setter Property="FontWeight" Value="Bold" />
        <!-- 靠左居中对齐 -->
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <!-- 外边距 -->
        <Setter Property="Margin" Value="0,0,0,15" />
    </Style>

    <!-- 3号标题 TextBlock -->
    <Style
        x:Key="TitleThreeUiTextBlockStyle"
        TargetType="{x:Type TextBlock}">
        <!-- 字号 -->
        <Setter Property="FontSize" Value="18" />
        <!-- 字体加粗 -->
        <Setter Property="FontWeight" Value="Bold" />
        <!-- 靠左居中对齐 -->
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <!-- 外边距 -->
        <Setter Property="Margin" Value="0,0,0,15" />
    </Style>

    <!-- 4号标题 TextBlock -->
    <Style
        x:Key="TitleFourUiTextBlockStyle"
        TargetType="{x:Type TextBlock}">
        <!-- 字号 -->
        <Setter Property="FontSize" Value="16" />
        <!-- 字体加粗 -->
        <Setter Property="FontWeight" Value="Bold" />
        <!-- 靠左居中对齐 -->
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <!-- 外边距 -->
        <Setter Property="Margin" Value="0,0,0,15" />
    </Style>

    <!-- 5号标题 TextBlock -->
    <Style
        x:Key="TitleFiveUiTextBlockStyle"
        TargetType="{x:Type TextBlock}">
        <!-- 字号 -->
        <Setter Property="FontSize" Value="13" />
        <!-- 字体加粗 -->
        <Setter Property="FontWeight" Value="Bold" />
        <!-- 靠左居中对齐 -->
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <!-- 外边距 -->
        <Setter Property="Margin" Value="0,0,0,15" />
    </Style>

    <!-- 6号标题 TextBlock -->
    <Style
        x:Key="TitleSixUiTextBlockStyle"
        TargetType="{x:Type TextBlock}">
        <!-- 字号 -->
        <Setter Property="FontSize" Value="14" />
        <!-- 字体加粗 -->
        <Setter Property="FontWeight" Value="Bold" />
        <!-- 靠左居中对齐 -->
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <!-- 外边距 -->
        <Setter Property="Margin" Value="0,0,0,15" />
    </Style>

    <!-- 侧边停靠的提示标签 TextBlock -->
    <Style
        x:Key="SideDockPromptUiTextBlockStyle"
        TargetType="{x:Type TextBlock}">
        <!-- 字号 -->
        <Setter Property="FontSize" Value="14" />
        <!-- 垂直水平居中对齐 -->
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <!-- 宽度 -->
        <Setter Property="Width" Value="60" />
    </Style>
</ResourceDictionary>