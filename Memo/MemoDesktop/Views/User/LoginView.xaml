<UserControl x:Class="MemoDesktop.Views.User.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MemoDesktop.Views.User"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:pass="clr-namespace:MemoDesktop.Extensions"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <!-- 登录界面的样式 -->
    <prism:Dialog.WindowStyle>
        <!-- 登录界面的样式 -->
        <Style TargetType="Window">
            <!-- 设置登录界面的长宽 -->
            <Setter Property="Width" Value="600" />
            <Setter Property="Height" Value="350" />
            <!-- 不允许调整窗体大小 -->
            <Setter Property="ResizeMode" Value="NoResize" />
            <!-- 启动的时候让窗体位于屏幕中间 -->
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterScreen" />
            <!-- 窗体标题 -->
            <Setter Property="Title" Value="{Binding Title}" />
        </Style>
    </prism:Dialog.WindowStyle>
    <Grid>
        <!-- 分两列 -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        
        <!-- 第一列内容：图片 -->
        <Image Grid.Column="0" Source="pack://application:,,,/MemoDesktop;component/Assets/Images/Login.jpg" Margin="20"/>
        
        <!-- 第二列内容 -->
        <StackPanel Grid.Column="1" Orientation="Vertical" Margin="15" VerticalAlignment="Center">
            <TextBlock
                Text="欢迎使用!"
                FontSize="22"
                FontWeight="Bold"
                Margin="0,10,0,0"
                />
            <!-- 用户输入框 -->
            <TextBox Margin="0,20,0,0" md:HintAssist.Hint="请输入用户名" Text="{Binding Account}" />
            <!-- 密码输入框 -->
            <!-- pass:PasswordExtension.Password="{Binding Password}" 通过附加属性去跟ViewModel绑定 -->
            <PasswordBox Margin="0,10,0,0" md:HintAssist.Hint="请输入密码..." pass:PasswordExtension.Password="{Binding Password}">
                <!-- Interaction.Behaviors - 附加属性 (Attached Property) -->
                <i:Interaction.Behaviors>
                    <pass:PasswordBehavior />
                </i:Interaction.Behaviors>
            </PasswordBox>
            <!-- 登录按钮 -->
            <Button Content="登录系统" Margin="0,20,0,0" Command="{Binding LoginCommand}"/>
            
            <!-- 注册账户和忘记密码 -->
            <DockPanel Margin="0,20,0,0" LastChildFill="False">
                <TextBlock Text="注册账户" DockPanel.Dock="Left"/>
                <TextBlock Text="忘记密码?" DockPanel.Dock="Right" />
            </DockPanel>
        </StackPanel>
    </Grid>
</UserControl>
