<Window x:Class="PrismEventAggregator.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrismEventAggregator.Views"
        xmlns:prism="http://prismlibrary.com/"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10">
            <TextBlock Text="事件发布者" FontSize="16" FontWeight="Bold"/>

            <Button Content="发布人员信息事件" 
                    Command="{Binding PublishEventCommand}"
                    Width="150" Height="30"/>

            <Border Background="LightGray" Padding="15" Margin="10">
                <StackPanel Margin="10">
                    <TextBlock Text="订阅者区域" FontWeight="Bold"/>
                    <ContentControl prism:RegionManager.RegionName="MainRegion" Height="200" Width="400"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Window>
